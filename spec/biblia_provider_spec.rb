#require 'spec_helper'
#require 'scripture_lookup/biblia_provider'

#describe ScriptureLookup::BibliaProvider do
  #before :each do
    #@provider = ScriptureLookup::BibliaProvider.new
  #end

  #describe "#new" do
    #it "returns a BibliaProvider instance" do
      #@provider.should be_an_instance_of ScriptureLookup::BibliaProvider
    #end
  #end

  #describe "#lookup" do
    #it "takes a ScriptureReference & returns an ESV scripture for John 3:16" do
      #@provider.lookup(make_ref("John 3:16"), :ESV).should eql "\"For God so loved the world"\
        #", that he gave his only Son, that whoever "\
        #"believes in him should not perish but have eternal life."
    #end
  #end
#end
